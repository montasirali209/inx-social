import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { AlertTriangle, Check, ChevronRight, CircleHelp, Download, ExternalLink, Link2, LoaderCircle, MoreVertical, Plus, RefreshCw, Search, ShieldCheck, Trash2, X } from 'lucide-react'
import { useMemo, useState } from 'react'
import { advancedHealthItems, platformMeta, supportedPlatforms, type ConnectionActivity, type ConnectionStatus, type Platform } from '../../data/connectedAccountsData'
import { relativeSyncTime } from '../../data/settingsData'
import { connectFacebook, connectOAuthPlatform, disconnectFacebookPage, disconnectSocialConnection, fetchConnectionsWorkspace, flattenConnectedIdentities, syncInstagram, type ConnectedIdentity } from '../../lib/connections-api'
import { useUiStore } from '../../store/ui-store'
import { Button } from '../ui/Button'

type Tab = 'all' | 'platforms' | 'profiles' | 'advanced'
type Notice = { tone: 'success' | 'error'; message: string } | null

function PlatformIcon({ platform, size = 'md' }: { platform: Platform; size?: 'sm' | 'md' | 'lg' }) {
  const meta = platformMeta[platform]
  const sizes = { sm: 'size-8 text-xs', md: 'size-10 text-sm', lg: 'size-12 text-base' }
  return <span aria-label={meta.label} className={`grid shrink-0 place-items-center rounded-xl font-black shadow-sm ${sizes[size]} ${meta.className}`}>{meta.mark}</span>
}

function StatusBadge({ status }: { status: ConnectionStatus }) {
  const labels: Record<ConnectionStatus, string> = { connected: 'Connected', syncing: 'Syncing', expiring_soon: 'Expiring soon', disconnected: 'Disconnected', permission_issue: 'Permission issue', reconnect_required: 'Reconnect required' }
  const tones: Record<ConnectionStatus, string> = { connected: 'bg-brand-green/10 text-brand-green', syncing: 'bg-brand-cyan/10 text-brand-cyan', expiring_soon: 'bg-brand-amber/10 text-brand-amber', disconnected: 'bg-brand-red/10 text-brand-red', permission_issue: 'bg-brand-amber/10 text-brand-amber', reconnect_required: 'bg-brand-red/10 text-brand-red' }
  return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[10px] font-semibold ${tones[status]}`}><span className="size-1.5 rounded-full bg-current" />{labels[status]}</span>
}

function Toggle({ checked, label, onChange }: { checked: boolean; label: string; onChange: (next: boolean) => void }) {
  return <button aria-checked={checked} aria-label={label} className={`relative h-6 w-11 rounded-full border transition focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-cyan ${checked ? 'border-brand-teal bg-brand-teal' : 'border-border-soft bg-bg'}`} onClick={() => onChange(!checked)} role="switch" type="button"><span className={`absolute top-0.5 size-4 rounded-full bg-white shadow transition ${checked ? 'left-6' : 'left-0.5'}`} /></button>
}

function Modal({ children, onClose, title }: { children: React.ReactNode; onClose: () => void; title: string }) {
  return <div className="fixed inset-0 z-50 grid place-items-center bg-black/75 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-label={title}><section className="max-h-[calc(100vh-2rem)] w-full max-w-xl overflow-y-auto rounded-2xl border border-border-soft bg-[#071923] p-5 shadow-2xl"><header className="flex items-start justify-between gap-3"><h2 className="text-lg font-semibold">{title}</h2><button aria-label="Close dialog" className="grid size-9 place-items-center rounded-lg text-text-muted hover:bg-white/5" onClick={onClose} type="button"><X className="size-4" /></button></header>{children}</section></div>
}

function EmptyState({ onConnect }: { onConnect: () => void }) {
  return <div className="grid min-h-72 place-items-center p-8 text-center"><div><Link2 className="mx-auto size-9 text-text-soft" /><strong className="mt-4 block text-base">No accounts connected yet.</strong><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-text-muted">Connect your first social platform to start publishing and scheduling content.</p><Button className="mt-5" onClick={onConnect}><Plus className="size-4" />Connect Account</Button></div></div>
}

export function ConnectedAccountsPage() {
  const queryClient = useQueryClient()
  const globalSearch = useUiStore((state) => state.connectionsSearch)
  const setGlobalSearch = useUiStore((state) => state.setConnectionsSearch)
  const workspace = useQuery({ queryKey: ['connections-workspace'], queryFn: fetchConnectionsWorkspace })
  const [tab, setTab] = useState<Tab>('all')
  const [notice, setNotice] = useState<Notice>(null)
  const [connectOpen, setConnectOpen] = useState(false)
  const [selectedPlatform, setSelectedPlatform] = useState<Platform | null>(null)
  const [managing, setManaging] = useState<ConnectedIdentity | null>(null)
  const [disconnecting, setDisconnecting] = useState<ConnectedIdentity | null>(null)
  const [securityOpen, setSecurityOpen] = useState(false)
  const [helpOpen, setHelpOpen] = useState(false)
  const [supportedOpen, setSupportedOpen] = useState(false)
  const [activityOpen, setActivityOpen] = useState(false)
  const [healthOpen, setHealthOpen] = useState(false)
  const [menuId, setMenuId] = useState<string | null>(null)
  const [toggles, setToggles] = useState<Record<string, { posts: boolean; scheduler: boolean; analytics: boolean; primary: boolean }>>({})
  const [autoSync, setAutoSync] = useState('When you refresh')
  const [tokenAlerts, setTokenAlerts] = useState(true)

  const identities = useMemo(() => workspace.data ? flattenConnectedIdentities(workspace.data) : [], [workspace.data])
  const filtered = useMemo(() => { const term = globalSearch.trim().toLowerCase(); return identities.filter((identity) => !term || `${identity.displayName} ${identity.username || ''} ${identity.detail} ${identity.platform} ${identity.status}`.toLowerCase().includes(term)) }, [identities, globalSearch])
  const counts = useMemo(() => Object.fromEntries((Object.keys(platformMeta) as Platform[]).map((platform) => [platform, identities.filter((identity) => identity.platform === platform).length])) as Record<Platform, number>, [identities])
  const activities = useMemo<ConnectionActivity[]>(() => identities.slice(0, 8).map((identity) => ({ id: identity.id, platform: identity.platform, accountName: identity.displayName, message: identity.status === 'connected' ? `${identity.displayName} is connected and available.` : `${identity.displayName} needs attention.`, status: identity.status === 'connected' ? 'success' : 'warning', createdAt: identity.lastSyncedAt || identity.connectedAt })), [identities])
  const refresh = async () => { await Promise.all([queryClient.invalidateQueries({ queryKey: ['connections-workspace'] }), queryClient.invalidateQueries({ queryKey: ['studio-overview'] }), queryClient.invalidateQueries({ queryKey: ['settings-workspace'] })]) }
  const connectMutation = useMutation({ mutationFn: async (platform: Platform) => { if (platform === 'facebook') return connectFacebook(); if (platform === 'instagram') return syncInstagram(); if (platform === 'linkedin' || platform === 'youtube' || platform === 'x') return connectOAuthPlatform(platform); throw new Error(`${platformMeta[platform].label} is not available yet.`) }, onSuccess: async (_, platform) => { await refresh(); setConnectOpen(false); setSelectedPlatform(null); setNotice({ tone: 'success', message: `${platformMeta[platform].label} connected successfully.` }) }, onError: (error) => setNotice({ tone: 'error', message: error instanceof Error ? error.message : 'The account could not be connected.' }) })
  const disconnectMutation = useMutation({ mutationFn: (identity: ConnectedIdentity) => identity.connectionId ? disconnectSocialConnection(identity.connectionId) : disconnectFacebookPage(identity.id), onSuccess: async () => { await refresh(); setDisconnecting(null); setManaging(null); setNotice({ tone: 'success', message: 'The connection was removed.' }) }, onError: (error) => setNotice({ tone: 'error', message: error instanceof Error ? error.message : 'The connection could not be removed.' }) })
  const syncMutation = useMutation({ mutationFn: async () => { await refresh() }, onSuccess: () => setNotice({ tone: 'success', message: 'Connection data refreshed successfully.' }), onError: () => setNotice({ tone: 'error', message: 'The connection health check could not complete.' }) })
  const defaultToggleState = { posts: true, scheduler: true, analytics: true, primary: false }
  const changeToggle = (identity: ConnectedIdentity, field: 'posts' | 'scheduler' | 'analytics' | 'primary', next: boolean) => setToggles((current) => ({ ...current, [identity.id]: { ...(current[identity.id] || defaultToggleState), [field]: next } }))
  const stateFor = (identity: ConnectedIdentity) => toggles[identity.id] || defaultToggleState
  const downloadLogs = () => { const csv = ['Platform,Account,Event,Status,Time', ...activities.map((entry) => [platformMeta[entry.platform].label, entry.accountName, entry.message, entry.status, entry.createdAt].map((value) => `"${String(value).replaceAll('"', '""')}"`).join(','))].join('\n'); const url = URL.createObjectURL(new Blob([csv], { type: 'text/csv' })); const link = document.createElement('a'); link.href = url; link.download = 'inx-social-connection-activity.csv'; link.click(); URL.revokeObjectURL(url); setNotice({ tone: 'success', message: 'Connection activity exported.' }) }

  if (workspace.isLoading) return <div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_25rem]"><div className="h-[36rem] animate-pulse rounded-2xl border border-border-soft bg-panel/55" /><div className="space-y-4"><div className="h-48 animate-pulse rounded-2xl border border-border-soft bg-panel/55" /><div className="h-64 animate-pulse rounded-2xl border border-border-soft bg-panel/55" /></div></div>
  if (workspace.isError || !workspace.data) return <section className="rounded-2xl border border-brand-red/25 bg-brand-red/7 p-6"><h2 className="text-lg font-semibold">Connected accounts unavailable</h2><p className="mt-2 text-sm text-text-muted">{workspace.error instanceof Error ? workspace.error.message : 'Refresh and try again.'}</p><Button className="mt-4" onClick={() => void workspace.refetch()}>Try again</Button></section>

  const platformCount = new Set(identities.map((identity) => identity.platform)).size
  const connected = identities.filter((identity) => identity.status === 'connected').length
  const attention = identities.length - connected
  const tabs: Array<[Tab, string]> = [['all', 'All Accounts'], ['platforms', 'Social Platforms'], ['profiles', 'Pages & Profiles'], ['advanced', 'Advanced']]
  // Every connected YouTube channel, social profile and Facebook Page is rendered as its own live identity.
  const connectedTable = <section className="overflow-hidden rounded-2xl border border-border-soft bg-panel/55 backdrop-blur-xl"><header className="flex flex-wrap items-center justify-between gap-3 border-b border-border-soft p-4 sm:p-5"><div><div className="flex items-center gap-2"><h2 className="text-lg font-semibold">Connected Platforms</h2><span className="rounded-full bg-brand-teal/10 px-2 py-1 text-[10px] font-bold text-brand-teal">{platformCount} Connected</span></div><p className="mt-1 text-xs text-text-muted">Live accounts authorised for your INXSocial workspace.</p></div><Button disabled={syncMutation.isPending} onClick={() => syncMutation.mutate()} variant="secondary"><RefreshCw className={`size-4 ${syncMutation.isPending ? 'animate-spin' : ''}`} />Sync now</Button></header>{filtered.length ? <><div className="hidden min-w-[48rem] grid-cols-[1.1fr_1.3fr_.85fr_.85fr_auto] gap-4 border-b border-border-soft px-5 py-3 text-[10px] font-bold uppercase tracking-[.12em] text-text-soft lg:grid"><span>Platform</span><span>Connected account</span><span>Status</span><span>Last sync</span><span>Actions</span></div><div className="divide-y divide-border-soft">{filtered.map((identity) => <article className="grid gap-3 p-4 transition hover:bg-panel-hover/35 lg:grid-cols-[1.1fr_1.3fr_.85fr_.85fr_auto] lg:items-center lg:gap-4 lg:px-5" key={`${identity.platform}-${identity.id}`}><div className="flex items-center gap-3"><PlatformIcon platform={identity.platform} /><span><strong className="block text-sm">{platformMeta[identity.platform].label}</strong><small className="text-xs text-text-muted">{identity.platform === 'facebook' ? 'Page' : identity.detail}</small></span></div><div className="flex min-w-0 items-center gap-2"><span className="grid size-8 shrink-0 place-items-center overflow-hidden rounded-lg border border-border-soft bg-bg/60 text-[10px]">{identity.avatarUrl ? <img alt="" className="size-full object-cover" src={identity.avatarUrl} /> : identity.displayName.slice(0, 2).toUpperCase()}</span><span className="min-w-0"><strong className="block truncate text-sm">{identity.displayName}</strong><small className="block truncate text-xs text-text-muted">{identity.username || 'Connected account'}</small></span></div><div><StatusBadge status={identity.status === 'connected' ? 'connected' : 'permission_issue'} /></div><span className="text-xs text-text-muted">{relativeSyncTime(identity.lastSyncedAt)}</span><div className="relative flex items-center gap-2"><Button onClick={() => setManaging(identity)} size="sm" variant="secondary">Manage</Button><button aria-label={`More actions for ${identity.displayName}`} className="grid size-9 place-items-center rounded-lg text-text-muted hover:bg-white/5 focus-visible:outline-2 focus-visible:outline-brand-cyan" onClick={() => setMenuId(menuId === identity.id ? null : identity.id)} type="button"><MoreVertical className="size-4" /></button>{menuId === identity.id && <div className="absolute right-0 top-10 z-10 w-44 rounded-xl border border-border-soft bg-[#071923] p-1 shadow-panel"><button className="menu-item" onClick={() => { setMenuId(null); syncMutation.mutate() }} type="button">Sync now</button><button className="menu-item" onClick={() => { setMenuId(null); setManaging(identity) }} type="button">View pages / profile</button><button className="menu-item" onClick={() => { setMenuId(null); setSelectedPlatform(identity.platform); setConnectOpen(true) }} type="button">Reconnect</button><a className="menu-item" href={identity.platform === 'facebook' ? 'https://facebook.com' : '#platform'} rel="noreferrer" target="_blank">Open platform <ExternalLink className="size-3" /></a><button className="menu-item text-brand-red" onClick={() => { setMenuId(null); setDisconnecting(identity) }} type="button">Disconnect</button></div>}</div></article>)}</div></> : <EmptyState onConnect={() => setConnectOpen(true)} />}</section>

  return <div className="space-y-4 pb-8"><label className="relative block sm:hidden"><span className="sr-only">Search accounts</span><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-text-muted" /><input className="min-h-11 w-full rounded-xl border border-border-soft bg-panel/70 pl-10 pr-3 text-sm text-text-main placeholder:text-text-soft focus:border-brand-cyan focus:outline-none" onChange={(event) => setGlobalSearch(event.target.value)} placeholder="Search accounts…" type="search" value={globalSearch} /></label><div className="flex flex-col gap-3 rounded-2xl border border-border-soft bg-panel/45 p-3 sm:flex-row sm:items-center sm:justify-between"><nav aria-label="Connected account sections" className="-mb-3 flex max-w-full overflow-x-auto"><div className="flex min-w-max gap-1">{tabs.map(([id, label]) => <button className={`border-b-2 px-3 py-3 text-sm font-medium transition focus-visible:outline-2 focus-visible:outline-brand-cyan ${tab === id ? 'border-brand-teal text-brand-teal' : 'border-transparent text-text-muted hover:text-white'}`} key={id} onClick={() => setTab(id)} type="button">{label}</button>)}</div></nav><Button className="w-full sm:w-auto" onClick={() => setConnectOpen(true)}><Plus className="size-4" />Connect Account</Button></div>
    {tab === 'all' && <><section className="flex flex-col gap-4 rounded-2xl border border-brand-teal/20 bg-[linear-gradient(120deg,rgba(20,184,166,.08),rgba(15,36,52,.7))] p-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-start gap-3"><span className="grid size-11 place-items-center rounded-xl bg-brand-teal/10 text-brand-teal"><ShieldCheck className="size-5" /></span><div><h2 className="font-semibold">Your accounts are secure</h2><p className="mt-1 text-sm text-text-muted">All connections are encrypted and your data is protected.</p></div></div><Button onClick={() => setSecurityOpen(true)} variant="secondary">Learn more <ChevronRight className="size-4" /></Button></section><div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_23rem]">{connectedTable}<aside className="space-y-4"><section className="rounded-2xl border border-border-soft bg-panel/55 p-5"><header className="flex items-center justify-between"><h2 className="font-semibold">Connection Overview</h2><button className="text-xs font-semibold text-brand-teal hover:text-brand-cyan" onClick={() => setHealthOpen(true)} type="button">View all</button></header><div className="mt-5 flex items-center gap-5"><div className="grid size-28 place-items-center rounded-full" style={{ background: `conic-gradient(#22c55e 0 ${identities.length ? connected / identities.length * 360 : 0}deg, #f59e0b 0 ${identities.length ? (connected + attention) / identities.length * 360 : 0}deg, rgba(148,163,184,.12) 0)` }}><div className="grid size-20 place-items-center rounded-full bg-panel text-center"><strong className="text-2xl">{connected}</strong><small className="text-[10px] text-text-muted">Active</small></div></div><dl className="space-y-2 text-xs"><div className="flex justify-between gap-7"><dt className="text-text-muted">Active</dt><dd>{connected}</dd></div><div className="flex justify-between gap-7"><dt className="text-text-muted">Needs attention</dt><dd>{attention}</dd></div><div className="flex justify-between gap-7"><dt className="text-text-muted">Platforms</dt><dd>{platformCount}</dd></div></dl></div><dl className="mt-5 space-y-2 border-t border-border-soft pt-4 text-xs"><div className="flex justify-between"><dt className="text-text-muted">Total accounts</dt><dd>{identities.length}</dd></div><div className="flex justify-between"><dt className="text-text-muted">Next sync</dt><dd>When refreshed</dd></div></dl></section><section className="rounded-2xl border border-border-soft bg-panel/55 p-5"><header className="flex items-center justify-between"><h2 className="font-semibold">Recent Activity</h2><button className="text-xs font-semibold text-brand-teal hover:text-brand-cyan" onClick={() => setActivityOpen(true)} type="button">View all</button></header><div className="mt-3 space-y-3">{activities.length ? activities.slice(0, 5).map((activity) => <div className="flex items-center gap-2.5" key={activity.id}><PlatformIcon platform={activity.platform} size="sm" /><p className="min-w-0 flex-1 text-xs leading-5"><span className="block truncate text-text-main">{activity.message}</span><small className="text-text-muted">{relativeSyncTime(activity.createdAt)}</small></p><Check className={`size-4 ${activity.status === 'success' ? 'text-brand-teal' : 'text-brand-amber'}`} /></div>) : <p className="py-6 text-center text-xs text-text-muted">Connection activity will appear here.</p>}</div></section><section className="rounded-2xl border border-border-soft bg-[linear-gradient(145deg,rgba(20,184,166,.08),rgba(10,27,45,.9))] p-5"><CircleHelp className="size-6 text-brand-teal" /><h2 className="mt-3 font-semibold">Need help connecting?</h2><p className="mt-2 text-sm leading-6 text-text-muted">Learn how to connect and troubleshoot your social accounts.</p><Button className="mt-4" onClick={() => setHelpOpen(true)} size="sm" variant="secondary">View Help Center <ExternalLink className="size-3.5" /></Button></section></aside></div><section className="flex flex-col gap-3 rounded-2xl border border-border-soft bg-panel/45 p-4 text-sm sm:flex-row sm:items-center sm:justify-between"><span className="text-text-muted">Don’t see your platform? We support 15+ platforms.</span><button className="font-semibold text-brand-teal hover:text-brand-cyan" onClick={() => setSupportedOpen(true)} type="button">View all supported platforms <ChevronRight className="inline size-4" /></button></section></>}
    {tab === 'platforms' && <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{supportedPlatforms(counts).map((platform) => <article className="rounded-2xl border border-border-soft bg-panel/55 p-5 transition hover:-translate-y-0.5 hover:border-brand-teal/30" key={platform.platform}><div className="flex items-start justify-between gap-3"><PlatformIcon platform={platform.platform} size="lg" /><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${platform.available ? 'bg-brand-teal/10 text-brand-teal' : 'bg-white/5 text-text-muted'}`}>{platform.available ? `${platform.connectedCount} connected` : 'Coming soon'}</span></div><h2 className="mt-4 font-semibold">{platform.label}</h2><p className="mt-1 min-h-10 text-sm leading-5 text-text-muted">{platform.description}</p><div className="mt-4 flex flex-wrap gap-1.5">{platform.supportedContentTypes.map((type) => <span className="rounded-full border border-border-soft px-2 py-1 text-[10px] text-text-muted" key={type}>{type}</span>)}</div><Button className="mt-5 w-full" disabled={!platform.available} onClick={() => { setSelectedPlatform(platform.platform); setConnectOpen(true) }} variant={platform.connectedCount ? 'secondary' : 'primary'}>{platform.available ? platform.connectedCount ? 'Manage / add account' : 'Connect' : 'Not available yet'}</Button></article>)}</section>}
    {tab === 'profiles' && <section className="overflow-hidden rounded-2xl border border-border-soft bg-panel/55"><header className="border-b border-border-soft p-5"><h2 className="font-semibold">Pages & Profiles</h2><p className="mt-1 text-sm text-text-muted">Choose where each live connection can be used inside INXSocial.</p></header>{filtered.length ? <div className="divide-y divide-border-soft">{filtered.map((identity) => { const value = stateFor(identity); return <article className="grid gap-4 p-4 xl:grid-cols-[minmax(14rem,1.5fr)_9rem_8rem_repeat(4,minmax(7rem,.7fr))_auto] xl:items-center" key={identity.id}><div className="flex min-w-0 items-center gap-3"><PlatformIcon platform={identity.platform} /><span className="min-w-0"><strong className="block truncate">{identity.displayName}</strong><small className="block truncate text-text-muted">{identity.username || identity.detail}</small></span></div><span className="text-sm text-text-muted">{platformMeta[identity.platform].label}</span><span className="text-sm text-text-muted">{identity.platform === 'facebook' ? 'Page' : identity.platform === 'youtube' ? 'Channel' : 'Profile'}</span><label className="flex items-center justify-between gap-2 text-xs">Default <Toggle checked={value.primary} label={`Set ${identity.displayName} as primary`} onChange={(next) => changeToggle(identity, 'primary', next)} /></label><label className="flex items-center justify-between gap-2 text-xs">Posts <Toggle checked={value.posts} label={`Enable ${identity.displayName} for Posts`} onChange={(next) => changeToggle(identity, 'posts', next)} /></label><label className="flex items-center justify-between gap-2 text-xs">Bulk Scheduler <Toggle checked={value.scheduler} label={`Enable ${identity.displayName} for Bulk Scheduler`} onChange={(next) => changeToggle(identity, 'scheduler', next)} /></label><label className="flex items-center justify-between gap-2 text-xs">Analytics <Toggle checked={value.analytics} label={`Enable ${identity.displayName} for Analytics`} onChange={(next) => changeToggle(identity, 'analytics', next)} /></label><div className="flex items-center justify-between gap-3"><StatusBadge status={identity.status === 'connected' ? 'connected' : 'permission_issue'} /><Button onClick={() => setManaging(identity)} size="sm" variant="secondary">Details</Button></div></article>})}</div> : <EmptyState onConnect={() => setConnectOpen(true)} />}</section>}
    {tab === 'advanced' && <div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_minmax(20rem,.7fr)]"><section className="rounded-2xl border border-border-soft bg-panel/55 p-5"><h2 className="font-semibold">Technical connection controls</h2><p className="mt-1 text-sm text-text-muted">Keep account information current and export a record of connection activity.</p><div className="mt-5 divide-y divide-border-soft"><div className="flex flex-wrap items-center justify-between gap-3 py-4"><div><strong className="text-sm">Manual Sync Now</strong><p className="mt-1 text-xs text-text-muted">Reload the secure connection record from INXSocial.</p></div><Button disabled={syncMutation.isPending} onClick={() => syncMutation.mutate()} size="sm" variant="secondary"><RefreshCw className={`size-3.5 ${syncMutation.isPending ? 'animate-spin' : ''}`} />Sync now</Button></div><label className="flex flex-wrap items-center justify-between gap-3 py-4"><span><strong className="text-sm">Auto Sync Frequency</strong><small className="mt-1 block text-xs text-text-muted">How often the dashboard should check connection data.</small></span><select className="min-h-10 rounded-xl border border-border-soft bg-bg/50 px-3 text-sm focus:border-brand-cyan focus:outline-none" onChange={(event) => setAutoSync(event.target.value)} value={autoSync}><option>When you refresh</option><option>Every 15 minutes</option><option>Hourly</option><option>Daily</option></select></label><div className="flex flex-wrap items-center justify-between gap-3 py-4"><span><strong className="text-sm">Token Expiry Alerts</strong><small className="mt-1 block text-xs text-text-muted">Show an alert if a platform asks you to reconnect.</small></span><Toggle checked={tokenAlerts} label="Enable token expiry alerts" onChange={setTokenAlerts} /></div>{advancedHealthItems.map(([title, status, description]) => <div className="flex flex-wrap items-center justify-between gap-3 py-4" key={title}><span><strong className="text-sm">{title}</strong><small className="mt-1 block text-xs text-text-muted">{description}</small></span><span className="rounded-full bg-brand-teal/10 px-2.5 py-1 text-xs font-semibold text-brand-teal">{status}</span></div>)}<div className="flex flex-wrap items-center justify-between gap-3 pt-4"><span><strong className="text-sm">Export Connection Logs</strong><small className="mt-1 block text-xs text-text-muted">Download the currently visible connection activity as a CSV.</small></span><Button onClick={downloadLogs} size="sm" variant="secondary"><Download className="size-3.5" />Export CSV</Button></div></div></section><section className="rounded-2xl border border-brand-red/25 bg-brand-red/5 p-5"><span className="grid size-10 place-items-center rounded-xl bg-brand-red/10 text-brand-red"><AlertTriangle className="size-5" /></span><h2 className="mt-4 font-semibold">Danger Zone</h2><p className="mt-2 text-sm leading-6 text-text-muted">Disconnecting stops future publishing and sync for that account. Existing post records remain available.</p><div className="mt-5 space-y-2"><Button className="w-full justify-between border-brand-red/30 bg-brand-red/10 text-brand-red hover:bg-brand-red/20" onClick={() => identities[0] && setDisconnecting(identities[0])} variant="secondary">Disconnect a platform <ChevronRight className="size-4" /></Button><Button className="w-full justify-between" disabled={!attention} onClick={() => setNotice({ tone: 'success', message: attention ? 'Review the accounts marked as needing attention.' : 'There are no expired connections to remove.' })} variant="secondary">Remove expired connections <ChevronRight className="size-4" /></Button><Button className="w-full justify-between" disabled={!activities.length} onClick={downloadLogs} variant="secondary">Export before clearing history <ChevronRight className="size-4" /></Button></div></section></div>}
    {connectOpen && <Modal onClose={() => { setConnectOpen(false); setSelectedPlatform(null) }} title={selectedPlatform ? `Connect ${platformMeta[selectedPlatform].label}` : 'Connect an account'}>{selectedPlatform ? <div className="mt-4"><div className="flex items-center gap-3"><PlatformIcon platform={selectedPlatform} size="lg" /><div><strong>{platformMeta[selectedPlatform].label}</strong><p className="mt-1 text-sm text-text-muted">{platformMeta[selectedPlatform].description}</p></div></div><section className="mt-5 rounded-xl border border-border-soft bg-bg/45 p-4"><h3 className="text-sm font-semibold">What INXSocial can access</h3><ul className="mt-3 space-y-2 text-sm text-text-muted"><li>• The account or Page identity you authorise</li><li>• Publishing and scheduling permissions where the platform provides them</li><li>• Connection and analytics information needed by this workspace</li></ul></section><p className="mt-4 text-xs leading-5 text-text-muted">INXSocial never receives your platform password. You can disconnect at any time.</p><div className="mt-5 grid gap-2 sm:grid-cols-2"><Button onClick={() => { setSelectedPlatform(null) }} variant="secondary">Back</Button><Button disabled={!platformMeta[selectedPlatform].available || connectMutation.isPending} onClick={() => connectMutation.mutate(selectedPlatform)}>{connectMutation.isPending ? <><LoaderCircle className="size-4 animate-spin" />Opening…</> : 'Continue securely'}</Button></div></div> : <div className="mt-4 grid gap-2 sm:grid-cols-2">{supportedPlatforms(counts).filter((platform) => !['threads', 'bluesky'].includes(platform.platform)).map((platform) => <button className="flex items-center gap-3 rounded-xl border border-border-soft bg-bg/35 p-3 text-left transition hover:border-brand-teal/35 disabled:cursor-not-allowed disabled:opacity-50" disabled={!platform.available} key={platform.platform} onClick={() => setSelectedPlatform(platform.platform)} type="button"><PlatformIcon platform={platform.platform} /><span><strong className="block text-sm">Connect {platform.label}</strong><small className="text-xs text-text-muted">{platform.available ? platform.connectedCount ? 'Add or reconnect' : 'Official connection' : 'Coming soon'}</small></span></button>)}</div>}</Modal>}
    {managing && <Modal onClose={() => setManaging(null)} title={`Manage ${managing.displayName}`}><div className="mt-4 space-y-4"><div className="flex items-center gap-3"><PlatformIcon platform={managing.platform} size="lg" /><div><strong>{managing.displayName}</strong><p className="text-sm text-text-muted">{managing.username || platformMeta[managing.platform].label}</p></div></div><dl className="grid grid-cols-2 gap-3 rounded-xl border border-border-soft bg-bg/35 p-4 text-sm"><div><dt className="text-text-muted">Last sync</dt><dd className="mt-1">{relativeSyncTime(managing.lastSyncedAt)}</dd></div><div><dt className="text-text-muted">Token status</dt><dd className="mt-1"><StatusBadge status={managing.status === 'connected' ? 'connected' : 'permission_issue'} /></dd></div></dl><section className="rounded-xl border border-border-soft bg-bg/35 p-4"><h3 className="text-sm font-semibold">Current permissions</h3><p className="mt-2 text-xs leading-5 text-text-muted">{platformMeta[managing.platform].description} Permissions are granted through the official platform authorisation screen.</p></section>{(['posts', 'scheduler', 'analytics'] as const).map((field) => <label className="flex items-center justify-between gap-3 border-b border-border-soft pb-3 text-sm" key={field}><span><strong>{field === 'posts' ? 'Publishing enabled' : field === 'scheduler' ? 'Scheduler enabled' : 'Analytics enabled'}</strong><small className="mt-1 block text-xs text-text-muted">Available for this connection in the matching workspace tool.</small></span><Toggle checked={stateFor(managing)[field]} label={`Toggle ${field} for ${managing.displayName}`} onChange={(next) => changeToggle(managing, field, next)} /></label>)}<div className="grid gap-2 sm:grid-cols-2"><Button onClick={() => syncMutation.mutate()} variant="secondary"><RefreshCw className="size-4" />Refresh / sync</Button><Button onClick={() => { setSelectedPlatform(managing.platform); setConnectOpen(true) }} variant="secondary">Reconnect</Button></div><Button className="w-full border-brand-red/30 bg-brand-red/10 text-brand-red hover:bg-brand-red/20" onClick={() => setDisconnecting(managing)} variant="secondary"><Trash2 className="size-4" />Disconnect account</Button></div></Modal>}
    {disconnecting && <Modal onClose={() => setDisconnecting(null)} title={`Disconnect ${disconnecting.displayName}?`}><div className="mt-4"><p className="text-sm leading-6 text-text-muted">This removes saved authorisation for this connection. It will no longer be available for future publishing, scheduling or sync. Existing post records stay intact.</p><label className="mt-4 block text-sm font-medium">Type <code className="rounded bg-white/10 px-1.5 py-0.5">DISCONNECT</code> to confirm<input className="mt-2 min-h-11 w-full rounded-xl border border-border-soft bg-bg/60 px-3 text-sm focus:border-brand-red focus:outline-none" onChange={(event) => { const target = document.getElementById('disconnect-confirm') as HTMLButtonElement | null; if (target) target.disabled = event.target.value !== 'DISCONNECT' }} placeholder="DISCONNECT" /></label><div className="mt-5 grid gap-2 sm:grid-cols-2"><Button onClick={() => setDisconnecting(null)} variant="secondary">Cancel</Button><Button className="border-brand-red/40 bg-brand-red/15 text-brand-red hover:bg-brand-red/25" disabled id="disconnect-confirm" onClick={() => disconnectMutation.mutate(disconnecting)}>{disconnectMutation.isPending ? 'Disconnecting…' : 'Disconnect'}</Button></div></div></Modal>}
    {securityOpen && <Modal onClose={() => setSecurityOpen(false)} title="How INXSocial protects connections"><ul className="mt-4 space-y-3 text-sm leading-6 text-text-muted"><li>• INXSocial never stores your social-account password.</li><li>• Connections use each platform’s official authorisation process.</li><li>• Access tokens are encrypted before they are stored.</li><li>• Only permissions needed for publishing, scheduling and analytics are requested.</li><li>• You can revoke access from INXSocial or the platform at any time.</li><li>• Disconnecting stops future publishing and sync immediately.</li></ul></Modal>}
    {helpOpen && <Modal onClose={() => setHelpOpen(false)} title="Connection help"><div className="mt-4 space-y-3">{['How to connect Facebook and Instagram', 'How to fix expired tokens', 'How to reconnect a disconnected account', 'Why permissions are required', 'What to do if a Page is missing', 'How to remove an account'].map((item) => <button className="flex w-full items-center justify-between rounded-xl border border-border-soft bg-bg/35 p-3 text-left text-sm hover:border-brand-teal/35" key={item} onClick={() => setNotice({ tone: 'success', message: 'Help guidance will open from the INXSocial Help Center.' })} type="button">{item}<ChevronRight className="size-4 text-brand-teal" /></button>)}</div></Modal>}
    {supportedOpen && <Modal onClose={() => setSupportedOpen(false)} title="Supported platforms"><div className="mt-4 grid gap-2 sm:grid-cols-2">{supportedPlatforms(counts).map((platform) => <div className="flex items-center gap-3 rounded-xl border border-border-soft bg-bg/35 p-3" key={platform.platform}><PlatformIcon platform={platform.platform} /><span><strong className="block text-sm">{platform.label}</strong><small className={platform.available ? 'text-brand-teal' : 'text-text-muted'}>{platform.available ? 'Available now' : 'Coming soon'}</small></span></div>)}</div></Modal>}
    {activityOpen && <Modal onClose={() => setActivityOpen(false)} title="Connection activity"><div className="mt-4 space-y-2">{activities.map((activity) => <article className="flex items-center gap-3 rounded-xl border border-border-soft bg-bg/35 p-3" key={activity.id}><PlatformIcon platform={activity.platform} /><span className="min-w-0 flex-1"><strong className="block text-sm">{activity.message}</strong><small className="text-xs text-text-muted">{activity.accountName} · {relativeSyncTime(activity.createdAt)}</small></span><Check className="size-4 text-brand-teal" /></article>)}</div></Modal>}
    {healthOpen && <Modal onClose={() => setHealthOpen(false)} title="Detailed account health"><div className="mt-4 space-y-3">{identities.map((identity) => <div className="flex items-center justify-between gap-3 rounded-xl border border-border-soft bg-bg/35 p-3" key={identity.id}><span className="flex items-center gap-3"><PlatformIcon platform={identity.platform} /><span><strong className="block text-sm">{identity.displayName}</strong><small className="text-xs text-text-muted">Last checked {relativeSyncTime(identity.lastSyncedAt)}</small></span></span><StatusBadge status={identity.status === 'connected' ? 'connected' : 'permission_issue'} /></div>)}</div></Modal>}
    {notice && <div className={`fixed bottom-5 right-5 z-[60] max-w-sm rounded-xl border px-4 py-3 text-sm shadow-2xl ${notice.tone === 'success' ? 'border-brand-teal/35 bg-[#08251f]/95' : 'border-brand-red/35 bg-[#30131b]/95'}`} role="status">{notice.message}</div>}
  </div>
}
