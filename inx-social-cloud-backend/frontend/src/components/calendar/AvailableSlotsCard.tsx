import type { AvailableSlot } from '../../types/calendar'

export function AvailableSlotsCard({ slots, selectedTime, onSelect }: { slots: AvailableSlot[]; selectedTime: string; onSelect: (time: string) => void }) {
  return <section className="rounded-card border border-border-soft bg-panel/55 p-3.5"><h3 className="text-xs font-semibold">Available Time Slots</h3><div className="mt-3 grid grid-cols-3 gap-2">{slots.map((slot) => <button aria-pressed={slot.time === selectedTime} className={`min-h-9 rounded-lg border px-1 text-[10px] font-semibold transition focus-visible:outline-2 focus-visible:outline-brand-cyan disabled:cursor-not-allowed disabled:opacity-35 ${slot.time === selectedTime ? 'border-brand-cyan bg-brand-cyan/12 text-brand-cyan' : 'border-border-soft bg-black/15 hover:border-brand-cyan/35 hover:bg-panel-hover'}`} disabled={!slot.available} key={slot.time} onClick={() => onSelect(slot.time)} title={slot.available ? 'Use this time in the post composer' : 'This time is already occupied'} type="button">{slot.label}</button>)}</div><p className="mt-2 text-[9px] leading-4 text-text-soft">Unavailable buttons are already occupied by retained scheduled content.</p></section>
}
