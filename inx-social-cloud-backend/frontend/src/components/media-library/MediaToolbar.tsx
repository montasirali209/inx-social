import { Grid2X2, List, Search, SlidersHorizontal } from 'lucide-react'

export function MediaToolbar({ search, onSearch, type, onType, sort, onSort, view, onView, onFilters }: { search: string; onSearch: (value: string) => void; type: string; onType: (value: string) => void; sort: string; onSort: (value: string) => void; view: 'grid' | 'list'; onView: (value: 'grid' | 'list') => void; onFilters: () => void }) {
  return <div className="flex flex-wrap gap-2"><button aria-label="Open filters" className="grid size-10 place-items-center rounded-xl border border-border-soft bg-panel text-text-muted hover:border-brand-cyan/35 hover:text-brand-cyan focus-visible:outline-2 focus-visible:outline-brand-cyan 2xl:hidden" onClick={onFilters} type="button"><SlidersHorizontal className="size-4" /></button><label className="relative min-w-[180px] flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-text-soft" /><input className="min-h-10 w-full rounded-xl border border-border-soft bg-panel/75 pl-9 pr-3 text-xs outline-none placeholder:text-text-soft focus:border-brand-cyan" onChange={event => onSearch(event.target.value)} placeholder="Search media…" value={search} /></label><select aria-label="Media type" className="min-h-10 rounded-xl border border-border-soft bg-panel/75 px-3 text-[10px] outline-none focus:border-brand-cyan" onChange={event => onType(event.target.value)} value={type}><option value="all">Type · All</option><option value="image">Images</option><option value="video">Videos</option><option value="gif">GIFs</option></select><select aria-label="Sort media" className="min-h-10 rounded-xl border border-border-soft bg-panel/75 px-3 text-[10px] outline-none focus:border-brand-cyan" onChange={event => onSort(event.target.value)} value={sort}><option value="newest">Sort · Newest</option><option value="oldest">Oldest</option><option value="name">Name</option><option value="size">File size</option></select><div className="flex rounded-xl border border-border-soft bg-panel/75 p-1"><button aria-label="Grid view" aria-pressed={view === 'grid'} className={`grid size-8 place-items-center rounded-lg ${view === 'grid' ? 'bg-brand-cyan/12 text-brand-cyan' : 'text-text-muted'}`} onClick={() => onView('grid')} type="button"><Grid2X2 className="size-4" /></button><button aria-label="List view" aria-pressed={view === 'list'} className={`grid size-8 place-items-center rounded-lg ${view === 'list' ? 'bg-brand-cyan/12 text-brand-cyan' : 'text-text-muted'}`} onClick={() => onView('list')} type="button"><List className="size-4" /></button></div></div>
}
